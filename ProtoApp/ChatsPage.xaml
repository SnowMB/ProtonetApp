<Page
    x:Class="ProtoApp.ChatsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProtoApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Chats, Source={StaticResource Locator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView ItemsSource="{Binding Chats}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Button Click="Button_Click">
                        <StackPanel>
                            <Image Source="{Binding OtherUsers[0].Avatar}"/>
                            <TextBlock FontWeight="Bold">ID</TextBlock>
                            <TextBlock Text="{Binding ID}"/>
                            <TextBlock FontWeight="Bold">Last Message</TextBlock>
                            <TextBlock Text="{Binding LastMeep.Message}"/>
                            <TextBlock FontWeight="Bold">Date</TextBlock>
                            <TextBlock Text="{Binding LastMeepDate}"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
